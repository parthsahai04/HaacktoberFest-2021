#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    priority_queue<int> pq;
    int x;
    for(int i=0;i<n;i++){
        cin>>x;
        pq.push(x);
    }
    while (!pq.empty())
    {
        /* code */
        cout<<pq.top()<<" ";
        pq.pop();
    }    
}
